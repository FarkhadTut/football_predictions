{
  "name": "football-predictor-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Football match outcome predictor monorepo (Phase 0)",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "dev:ui": "pnpm --filter ui dev"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.17.1"
}
