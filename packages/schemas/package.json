{
  "name": "@football-predictor/schemas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'no lint targets yet' && exit 0",
    "test": "echo 'no tests yet' && exit 0",
    "typecheck": "tsc --noEmit",
    "build": "echo 'no build yet' && exit 0",
    "generate": "openapi-typescript ../../apps/predictor/openapi.json -o ./src/openapi.d.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
